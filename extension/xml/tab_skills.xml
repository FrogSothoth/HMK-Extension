<?xml version="1.0" encoding="iso-8859-1"?>
<root>
	<!--
		HarnMaster Skills Tab
		6 skill categories stacked vertically: Social, Lore, Physical, Nature, Craft, Combat
		Columns: SKILL, ATT1, ATT2, SM, SB, ML

		Each skill group auto-sizes to fit all skills (no internal scrolling).
		Tab scrolls vertically if content exceeds available space.
	-->

	<!-- Template for a single skill row -->
	<windowclass name="harn_skill_entry">
		<margins control="0,0,0,2" />
		<script>
			function onInit()
				-- Add handlers for name update
				local node = getDatabaseNode();
				if node then
					DB.addHandler(node.getPath() .. ".name", "onUpdate", onNameUpdate);
				end
			end

			function onClose()
				local node = getDatabaseNode();
				if node then
					DB.removeHandler(node.getPath() .. ".name", "onUpdate", onNameUpdate);
				end
			end

			function onNameUpdate()
				-- Name updates are now handled by standard DB triggers if needed
			end
		</script>
		<sheetdata>
			<stringfield name="name">
				<anchored position="insidetopleft" offset="3,0" width="85" height="20" />
				<frame name="fielddark" offset="7,5,7,5" />
				<script>
					function onLoseFocus()
						local sName = getValue();
						if sName == "" then return; end

						Debug.console("Skill name entered: " .. sName);

						-- Look up skill data from database
						local nodeSkillsRef = DB.findNode("harnmaster.skills");
						if nodeSkillsRef then
							for _, nodeRefSkill in pairs(DB.getChildren(nodeSkillsRef)) do
								if DB.getValue(nodeRefSkill, "name", "") == sName then
									local sAtt1 = DB.getValue(nodeRefSkill, "att1", "");
									local sAtt2 = DB.getValue(nodeRefSkill, "att2", "");
									local nSM = DB.getValue(nodeRefSkill, "sm", 0);

									Debug.console("Found skill data: att1=" .. sAtt1 .. ", att2=" .. sAtt2 .. ", sm=" .. tostring(nSM));

									-- Update att1, att2, sm fields in character's skill entry
									local nodeCharSkill = window.getDatabaseNode();
									if nodeCharSkill then
										Debug.console("Setting database values for: " .. nodeCharSkill.getPath());
										DB.setValue(nodeCharSkill, "att1", "string", sAtt1);
										DB.setValue(nodeCharSkill, "att2", "string", sAtt2);
										DB.setValue(nodeCharSkill, "sm", "number", nSM);

										-- Calculate SB from character attributes
										-- Navigate to character node (skill -> skilllist -> character)
										local nodeChar = nodeCharSkill.getParent().getParent();
										if nodeChar then
											-- Get attribute scores (field names are lowercase + "_score")
											local sAtt1Field = string.lower(sAtt1) .. "_score";
											local sAtt2Field = string.lower(sAtt2) .. "_score";
											local nAtt1Value = DB.getValue(nodeChar, sAtt1Field, 0);
											local nAtt2Value = DB.getValue(nodeChar, sAtt2Field, 0);

											Debug.console("Attribute values: " .. sAtt1 .. "=" .. tostring(nAtt1Value) .. ", " .. sAtt2 .. "=" .. tostring(nAtt2Value));

											-- Calculate SB: average, round up if att1 > att2, else round down
											local nSB = 0;
											if nAtt1Value > 0 or nAtt2Value > 0 then
												local nAvg = (nAtt1Value + nAtt2Value) / 2;
												if nAtt1Value > nAtt2Value then
													nSB = math.ceil(nAvg);
												else
													nSB = math.floor(nAvg);
												end
											end

											Debug.console("Calculated SB: " .. tostring(nSB));
											DB.setValue(nodeCharSkill, "sb", "number", nSB);
										else
											Debug.console("ERROR: Could not get character node");
										end
									else
										Debug.console("ERROR: Could not get database node");
									end
									return;
								end
							end
							Debug.console("Skill not found in harnmaster.skills: " .. sName);
						else
							Debug.console("ERROR: harnmaster.skills node not found");
						end
					end
				</script>
			</stringfield>
			<stringfield name="att1">
				<anchored to="name" position="righthigh" offset="6,0" width="20" height="20" />
				<font>att_text</font>
				<center />
				<readonly />
				<source>att1</source>
			</stringfield>
			<stringfield name="att2">
				<anchored to="att1" position="righthigh" offset="3,0" width="20" height="20" />
				<font>att_text</font>
				<center />
				<readonly />
				<source>att2</source>
			</stringfield>
			<numberfield name="sm">
				<anchored to="name" position="righthigh" offset="53,0" width="20" height="20" />
				<frame name="fielddark" offset="7,5,7,5" />
				<font>sheettext</font>
				<center />
				<readonly />
				<source>sm</source>
			</numberfield>
			<numberfield name="sb">
				<anchored to="sm" position="righthigh" offset="3,0" width="25" height="20" />
				<font>sheettext</font>
				<center />
				<readonly />
				<source>sb</source>
			</numberfield>
			<numberfield name="ml">
				<anchored to="sm" position="righthigh" offset="30,0" width="30" height="20" />
				<frame name="fielddark" offset="7,5,7,5" />
				<font>sheettext</font>
				<center />
				<source>ml</source>
			</numberfield>

		</sheetdata>
	</windowclass>

	<!-- Individual skill group windowclass - auto-sizes to content -->
	<windowclass name="harn_skill_group">
		<margins control="0,0,0,5" />
		<sheetdata>
			<anchor_column name="columnanchor" />

			<!-- Group Title -->
			<label name="title">
				<anchored height="20">
					<top parent="columnanchor" anchor="bottom" relation="relative" offset="5" />
					<left offset="10" />
				</anchored>
				<font>sheetlabel</font>
			</label>

			<!-- Column Headers -->
			<label name="h_skill">
				<anchored height="20">
					<top parent="columnanchor" anchor="bottom" relation="relative" offset="2" />
					<left offset="5" />
				</anchored>
				<static>SKILL</static>
				<font>sheetlabel</font>
			</label>
			<!-- <label name="h_att1">
				<anchored to="h_skill" position="righthigh" offset="5,0" width="15" height="20" />
				<static>ATT1</static>
				<font>sheetlabel</font>
				<center />
			</label>
			<label name="h_att2">
				<anchored to="h_att1" position="righthigh" offset="5,0" width="15" height="20" />
				<static>ATT2</static>
				<font>sheetlabel</font>
				<center />
			</label> -->
			<label name="h_sm">
				<anchored to="h_skill" position="righthigh" offset="106,0" width="30" height="20" />
				<static>SM</static>
				<font>sheetlabel</font>
				<center />
			</label>
			<label name="h_sb">
				<anchored to="h_sm" position="righthigh" offset="3,0" width="20" height="20" />
				<static>SB</static>
				<font>sheetlabel</font>
				<center />
			</label>
			<label name="h_ml">
				<anchored to="h_sb" position="righthigh" offset="2,0" width="35" height="20" />
				<static>ML</static>
				<font>sheetlabel</font>
				<center />
			</label>

			<!-- Skills List - no bottom anchor, uses noscroll to show all items -->
			<windowlist name="skills">
				<anchored>
					<top parent="columnanchor" anchor="bottom" relation="relative" offset="2" />
					<left offset="5" />
					<right offset="-5" />
				</anchored>
				<class>harn_skill_entry</class>
				<noscroll />
				<allowcreate />
				<allowdelete />
			</windowlist>
		</sheetdata>
	</windowclass>

	<windowclass name="harn_tab_skills">
		<script>
			-- Helper function to check if a skill already exists in a list
			function skillExists(nodeSkillList, sSkillName)
				if not nodeSkillList then return false; end
				for _, nodeSkill in pairs(DB.getChildren(nodeSkillList)) do
					if DB.getValue(nodeSkill, "name", "") == sSkillName then
						return true;
					end
				end
				return false;
			end

			function onRefresh()
				local nodeChar = getDatabaseNode();
				if nodeChar and SkillsManager and SkillsManager.recalculateAllSB then
					SkillsManager.recalculateAllSB(nodeChar);
				end

				-- Iterate through all skill lists and force entries to update their special fields
				local tLists = { socialskills, loreskills, physicalskills, natureskills, craftskills, combatskills };
				for _, list in ipairs(tLists) do
					for _, window in pairs(list.getWindows()) do
						if window.onNameUpdate then
							window.onNameUpdate();
						end
						if window.onLanguageUpdate then
							window.onLanguageUpdate();
						end
					end
				end

				-- Update sunsign modifiers in category labels
				updateCategoryLabels();
			end

			function updateCategoryLabels()
				local nodeChar = getDatabaseNode();
				if not nodeChar then return; end

				local sSunsign = DB.getValue(nodeChar, "birth_sunsign", "");
				local tMods = HarnManager.getSunsignModifiers(sSunsign);
				-- tMods is {social, lore, physical, nature, craft, combat}

				local function formatMod(n)
					if n &gt; 0 then return "[+" .. n .. "]"; end
					if 0 &gt; n then return "[" .. n .. "]"; end
					return "[0]";
				end

				if social_title then
					social_title.setValue(Interface.getString("harn_skill_social") .. " " .. formatMod(tMods[1]));
				end
				if lore_title then
					lore_title.setValue(Interface.getString("harn_skill_lore") .. " " .. formatMod(tMods[2]));
				end
				if physical_title then
					physical_title.setValue(Interface.getString("harn_skill_physical") .. " " .. formatMod(tMods[3]));
				end
				if nature_title then
					nature_title.setValue(Interface.getString("harn_skill_nature") .. " " .. formatMod(tMods[4]));
				end
				if craft_title then
					craft_title.setValue(Interface.getString("harn_skill_craft") .. " " .. formatMod(tMods[5]));
				end
				if combat_title then
					combat_title.setValue(Interface.getString("harn_skill_combat") .. " " .. formatMod(tMods[6]));
				end
			end

			function onInit()
				-- Auto-populate default skills (sm > 0) for new characters
				local nodeChar = getDatabaseNode();
				if nodeChar then
					DB.addHandler(nodeChar.getPath() .. ".birth_sunsign", "onUpdate", updateCategoryLabels);
				end
				updateCategoryLabels();
				if not nodeChar then return; end

				-- Check if skills have already been initialized using a flag
				local bInitialized = DB.getValue(nodeChar, "skills_initialized", 0);
				if bInitialized == 1 then
					Debug.console("Skills already initialized, skipping auto-populate");
					return;
				end

				-- Set the flag immediately to prevent duplicate runs
				DB.setValue(nodeChar, "skills_initialized", "number", 1);

				Debug.console("Auto-populating default skills for new character");

				-- Group to datasource mapping
				local tGroupMap = {
					["Social"] = "socialskills",
					["Lore"] = "loreskills",
					["Physical"] = "physicalskills",
					["Nature"] = "natureskills",
					["Craft"] = "craftskills",
					["Combat"] = "combatskills"
				};

				-- Get skills reference from database
				local nodeSkillsRef = DB.findNode("harnmaster.skills");
				if not nodeSkillsRef then
					Debug.console("ERROR: harnmaster.skills not found");
					return;
				end

				-- Track skills we've already added this run to prevent duplicates
				local tAddedSkills = {};

				-- Skills to always include as defaults (even with SM=0)
				local tAlwaysInclude = {
					["Language"] = true,
					["Ritual"] = true
				};

				-- Iterate through all skills
				for _, nodeRefSkill in pairs(DB.getChildren(nodeSkillsRef)) do
					local sName = DB.getValue(nodeRefSkill, "name", "");
					local nSM = DB.getValue(nodeRefSkill, "sm", 0);
					if nSM > 0 or tAlwaysInclude[sName] then
						local sGroup = DB.getValue(nodeRefSkill, "group", "");
						local sAtt1 = DB.getValue(nodeRefSkill, "att1", "");
						local sAtt2 = DB.getValue(nodeRefSkill, "att2", "");

						-- Find the right skill list for this group
						local sListName = tGroupMap[sGroup];
						if sListName then
							-- Create a unique key for this skill
							local sKey = sListName .. ":" .. sName;

							-- Skip if we already added this skill in this run
							if tAddedSkills[sKey] then
								Debug.console("Skipping duplicate: " .. sName);
							else
								-- Also check if it exists in the database already
								local nodeSkillList = DB.createChild(nodeChar, sListName);
								if not skillExists(nodeSkillList, sName) then
									Debug.console("Adding default skill: " .. sName .. " to " .. sListName);

									local nodeNewSkill = DB.createChild(nodeSkillList);

									DB.setValue(nodeNewSkill, "name", "string", sName);
									DB.setValue(nodeNewSkill, "att1", "string", sAtt1);
									DB.setValue(nodeNewSkill, "att2", "string", sAtt2);
									DB.setValue(nodeNewSkill, "sm", "number", nSM);

									-- Calculate SB from character attributes
									local sAtt1Field = string.lower(sAtt1) .. "_score";
									local sAtt2Field = string.lower(sAtt2) .. "_score";
									local nAtt1Value = DB.getValue(nodeChar, sAtt1Field, 0);
									local nAtt2Value = DB.getValue(nodeChar, sAtt2Field, 0);

									local nSB = 0;
									if nAtt1Value > 0 or nAtt2Value > 0 then
										local nAvg = (nAtt1Value + nAtt2Value) / 2;
										if nAtt1Value > nAtt2Value then
											nSB = math.ceil(nAvg);
										else
											nSB = math.floor(nAvg);
										end
									end
									DB.setValue(nodeNewSkill, "sb", "number", nSB);

									-- Mark as added
									tAddedSkills[sKey] = true;
								else
									Debug.console("Skill already exists in DB: " .. sName);
								end
							end
						end
					end
				end

				Debug.console("Default skills auto-population complete");
			end

			function onClose()
				local nodeChar = getDatabaseNode();
				if nodeChar then
					DB.removeHandler(nodeChar.getPath() .. ".birth_sunsign", "onUpdate", updateCategoryLabels);
				end
			end
		</script>
		<sheetdata>
			<!-- Use anchor_column for vertical flow -->
			<anchor_column name="columnanchor" />

			<!-- SOCIAL Skills Group Box -->
			<genericcontrol name="social_group_box">
				<anchored width="275" height="200">
					<top parent="columnanchor" anchor="bottom" relation="relative" offset="15" />
					<left offset="40" />
				</anchored>
				<frame name="groupbox" />
			</genericcontrol>

			<label name="social_title">
				<anchored to="social_group_box" position="insidetop" offset="0,10" width="100" height="20" />
				<static textres="harn_skill_social" />
				<font>harn_group</font>
				<center />
			</label>

			<button_iadd name="socialskills_iadd">
				<anchored to="social_group_box" position="insidetopright" offset="25,10" width="15" height="15" />
				<target>socialskills</target>
			</button_iadd>

			<label name="social_h_skill">
				<anchored to="social_group_box" position="insidetopleft" offset="13,40" width="30" height="20" />
				<static>SKILL</static>
				<font>harn_skill_header</font>
			</label>
			<label name="social_h_sm">
				<anchored to="social_h_skill" position="righthigh" offset="106,0" width="30" height="20" />
				<static>SM</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="social_h_sb">
				<anchored to="social_h_sm" position="righthigh" offset="3,0" width="20" height="20" />
				<static>SB</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="social_h_ml">
				<anchored to="social_h_sb" position="righthigh" offset="2,0" width="35" height="20" />
				<static>ML</static>
				<font>harn_skill_header</font>
				<center />
			</label>

			<windowlist name="socialskills">
				<anchored to="social_group_box">
					<top offset="65" />
					<left offset="15" />
					<right offset="-15" />
					<bottom offset="-15" />
				</anchored>
				<datasource>.socialskills</datasource>
				<class>harn_skill_entry</class>
				<sortby><field>name</field></sortby>
				<allowcreate />
				<allowdelete />
			</windowlist>

			<!-- LORE Skills Group Box (Right of Social) -->
			<genericcontrol name="lore_group_box">
				<anchored width="275" height="200">
					<top parent="social_group_box" anchor="top" />
					<right offset="-40" />
				</anchored>
				<frame name="groupbox" />
			</genericcontrol>

			<label name="lore_title">
				<anchored to="lore_group_box" position="insidetop" offset="0,10" width="100" height="20" />
				<static textres="harn_skill_lore" />
				<font>harn_group</font>
				<center />
			</label>

			<button_iadd name="loreskills_iadd">
				<anchored to="lore_group_box" position="insidetopright" offset="25,10" width="15" height="15" />
				<target>loreskills</target>
			</button_iadd>

			<label name="lore_h_skill">
				<anchored to="lore_group_box" position="insidetopleft" offset="13,40" width="30" height="20" />
				<static>SKILL</static>
				<font>harn_skill_header</font>
			</label>
			<label name="lore_h_sm">
				<anchored to="lore_h_skill" position="righthigh" offset="106,0" width="30" height="20" />
				<static>SM</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="lore_h_sb">
				<anchored to="lore_h_sm" position="righthigh" offset="3,0" width="20" height="20" />
				<static>SB</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="lore_h_ml">
				<anchored to="lore_h_sb" position="righthigh" offset="2,0" width="35" height="20" />
				<static>ML</static>
				<font>harn_skill_header</font>
				<center />
			</label>

			<windowlist name="loreskills">
				<anchored to="lore_group_box">
					<top offset="65" />
					<left offset="15" />
					<right offset="-15" />
					<bottom offset="-15" />
				</anchored>
				<datasource>.loreskills</datasource>
				<class>harn_skill_entry</class>
				<sortby><field>name</field></sortby>
				<allowcreate />
				<allowdelete />
			</windowlist>

			<!-- PHYSICAL Skills Group Box (Under Social) -->
			<genericcontrol name="physical_group_box">
				<anchored width="275" height="200">
					<top parent="columnanchor" anchor="bottom" relation="relative" offset="15" />
					<left offset="40" />
				</anchored>
				<frame name="groupbox" />
			</genericcontrol>

			<label name="physical_title">
				<anchored to="physical_group_box" position="insidetop" offset="0,10" width="100" height="20" />
				<static textres="harn_skill_physical" />
				<font>harn_group</font>
				<center />
			</label>

			<button_iadd name="physicalskills_iadd">
				<anchored to="physical_group_box" position="insidetopright" offset="25,10" width="15" height="15" />
				<target>physicalskills</target>
			</button_iadd>

			<label name="physical_h_skill">
				<anchored to="physical_group_box" position="insidetopleft" offset="13,40" width="30" height="20" />
				<static>SKILL</static>
				<font>harn_skill_header</font>
			</label>
			<label name="physical_h_sm">
				<anchored to="physical_h_skill" position="righthigh" offset="106,0" width="30" height="20" />
				<static>SM</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="physical_h_sb">
				<anchored to="physical_h_sm" position="righthigh" offset="3,0" width="20" height="20" />
				<static>SB</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="physical_h_ml">
				<anchored to="physical_h_sb" position="righthigh" offset="2,0" width="35" height="20" />
				<static>ML</static>
				<font>harn_skill_header</font>
				<center />
			</label>

			<windowlist name="physicalskills">
				<anchored to="physical_group_box">
					<top offset="65" />
					<left offset="15" />
					<right offset="-15" />
					<bottom offset="-15" />
				</anchored>
				<datasource>.physicalskills</datasource>
				<class>harn_skill_entry</class>
				<sortby><field>name</field></sortby>
				<allowcreate />
				<allowdelete />
			</windowlist>

			<!-- NATURE Skills Group Box (Right of Physical) -->
			<genericcontrol name="nature_group_box">
				<anchored width="275" height="200">
					<top parent="physical_group_box" anchor="top" />
					<right offset="-40" />
				</anchored>
				<frame name="groupbox" />
			</genericcontrol>

			<label name="nature_title">
				<anchored to="nature_group_box" position="insidetop" offset="0,10" width="100" height="20" />
				<static textres="harn_skill_nature" />
				<font>harn_group</font>
				<center />
			</label>

			<button_iadd name="natureskills_iadd">
				<anchored to="nature_group_box" position="insidetopright" offset="25,10" width="15" height="15" />
				<target>natureskills</target>
			</button_iadd>

			<label name="nature_h_skill">
				<anchored to="nature_group_box" position="insidetopleft" offset="13,40" width="30" height="20" />
				<static>SKILL</static>
				<font>harn_skill_header</font>
			</label>
			<label name="nature_h_sm">
				<anchored to="nature_h_skill" position="righthigh" offset="106,0" width="30" height="20" />
				<static>SM</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="nature_h_sb">
				<anchored to="nature_h_sm" position="righthigh" offset="3,0" width="20" height="20" />
				<static>SB</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="nature_h_ml">
				<anchored to="nature_h_sb" position="righthigh" offset="2,0" width="35" height="20" />
				<static>ML</static>
				<font>harn_skill_header</font>
				<center />
			</label>

			<windowlist name="natureskills">
				<anchored to="nature_group_box">
					<top offset="65" />
					<left offset="15" />
					<right offset="-15" />
					<bottom offset="-15" />
				</anchored>
				<datasource>.natureskills</datasource>
				<class>harn_skill_entry</class>
				<sortby><field>name</field></sortby>
				<allowcreate />
				<allowdelete />
			</windowlist>

			<!-- CRAFT Skills Group Box (Under Physical) -->
			<genericcontrol name="craft_group_box">
				<anchored width="275" height="200">
					<top parent="columnanchor" anchor="bottom" relation="relative" offset="15" />
					<left offset="40" />
				</anchored>
				<frame name="groupbox" />
			</genericcontrol>

			<label name="craft_title">
				<anchored to="craft_group_box" position="insidetop" offset="0,10" width="100" height="20" />
				<static textres="harn_skill_craft" />
				<font>harn_group</font>
				<center />
			</label>

			<button_iadd name="craftskills_iadd">
				<anchored to="craft_group_box" position="insidetopright" offset="25,10" width="15" height="15" />
				<target>craftskills</target>
			</button_iadd>

			<label name="craft_h_skill">
				<anchored to="craft_group_box" position="insidetopleft" offset="13,40" width="30" height="20" />
				<static>SKILL</static>
				<font>harn_skill_header</font>
			</label>
			<label name="craft_h_sm">
				<anchored to="craft_h_skill" position="righthigh" offset="106,0" width="30" height="20" />
				<static>SM</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="craft_h_sb">
				<anchored to="craft_h_sm" position="righthigh" offset="3,0" width="20" height="20" />
				<static>SB</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="craft_h_ml">
				<anchored to="craft_h_sb" position="righthigh" offset="2,0" width="35" height="20" />
				<static>ML</static>
				<font>harn_skill_header</font>
				<center />
			</label>

			<windowlist name="craftskills">
				<anchored to="craft_group_box">
					<top offset="65" />
					<left offset="15" />
					<right offset="-15" />
					<bottom offset="-15" />
				</anchored>
				<datasource>.craftskills</datasource>
				<class>harn_skill_entry</class>
				<sortby><field>name</field></sortby>
				<allowcreate />
				<allowdelete />
			</windowlist>

			<!-- COMBAT Skills Group Box (Right of Craft) -->
			<genericcontrol name="combat_group_box">
				<anchored width="275" height="200">
					<top parent="craft_group_box" anchor="top" />
					<right offset="-40" />
				</anchored>
				<frame name="groupbox" />
			</genericcontrol>

			<label name="combat_title">
				<anchored to="combat_group_box" position="insidetop" offset="0,10" width="100" height="20" />
				<static textres="harn_skill_combat" />
				<font>harn_group</font>
				<center />
			</label>

			<button_iadd name="combatskills_iadd">
				<anchored to="combat_group_box" position="insidetopright" offset="25,10" width="15" height="15" />
				<target>combatskills</target>
			</button_iadd>

			<label name="combat_h_skill">
				<anchored to="combat_group_box" position="insidetopleft" offset="13,40" width="30" height="20" />
				<static>SKILL</static>
				<font>harn_skill_header</font>
			</label>
			<label name="combat_h_sm">
				<anchored to="combat_h_skill" position="righthigh" offset="106,0" width="30" height="20" />
				<static>SM</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="combat_h_sb">
				<anchored to="combat_h_sm" position="righthigh" offset="3,0" width="20" height="20" />
				<static>SB</static>
				<font>harn_skill_header</font>
				<center />
			</label>
			<label name="combat_h_ml">
				<anchored to="combat_h_sb" position="righthigh" offset="2,0" width="35" height="20" />
				<static>ML</static>
				<font>harn_skill_header</font>
				<center />
			</label>

			<windowlist name="combatskills">
				<anchored to="combat_group_box">
					<top offset="65" />
					<left offset="15" />
					<right offset="-15" />
					<bottom offset="-15" />
				</anchored>
				<datasource>.combatskills</datasource>
				<class>harn_skill_entry</class>
				<sortby><field>name</field></sortby>
				<allowcreate />
				<allowdelete />
			</windowlist>
		</sheetdata>
	</windowclass>
</root>
